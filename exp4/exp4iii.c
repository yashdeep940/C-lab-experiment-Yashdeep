#include<stdio.h>
int main(){
    {
        int y=20;
        printf("Inside block:x=%d",y=%d\n",x,y);
    }
    //printf("outside block:x=%d,y=%d",x,y); will give compilation error
    return 0;
}